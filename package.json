{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "nodemon server.js",
    "admin": "npm run start --prefix-admin",
    "site": "npm run start --prefix-site",
    "build-admin": "cd admin && npm install && npm run build",
    "build-site": "cd site && npm install && npm run build",
    "dev": "concurrently \"npm run build-admin\" \"npm run build-site\" \"npm run server\"",
    "heroku-postbuild": "npm run build-admin"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@material-ui/icons": "^4.11.2",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "joi": "^17.4.2",
    "mongoose": "^5.13.2",
    "multer": "^1.4.3",
    "slugify": "^1.5.3",
    "speakingurl": "^14.0.1"
  },
  "devDependencies": {
    "concurrently": "^6.3.0",
    "nodemon": "^2.0.10"
  }
}
